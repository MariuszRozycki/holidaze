import { Container } from "react-bootstrap";
import { RenderLogInAsManager } from "../../components";

const LoginAsManagerPage = () => {
  return (
    <Container>
      <RenderLogInAsManager />
    </Container>
  );
};

export default LoginAsManagerPage;
